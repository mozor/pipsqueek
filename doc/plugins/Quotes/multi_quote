quote [<id | phrase>]

Returns the quote referenced by 'id', or tries to find a quote matching
'phrase'.  If no arguments are given, returns a random quote from the database.  You can also use the special variable '_' in place of the id.

